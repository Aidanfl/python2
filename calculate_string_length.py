# This script calculates and prints the length of the string "banana".
# It uses the built-in len() function to determine the number of characters in the string.

# length
s = "banana"
length = len(s)
print(length)
