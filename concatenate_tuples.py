# This script demonstrates how to concatenate two tuples.
# It creates two tuples: 'a' with values (32, 6, "sky") and 'b' with values (3, 8, 7).
# It concatenates these tuples and stores the result in 'c', then prints the concatenated tuple.

a = (32, 6, "sky")
b = (3, 8, 7)

c = a +b

print(c)
