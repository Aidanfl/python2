# This script displays the numbers from 0 to 99 using a for loop.
# It prints each number in the range of 0 to 99.
# (Note: The range(0,100) excludes 100, so it will print up to 99).

# display 0, 1, 2, 3, 4, ..., 100 using while statement

for n in range(0,100):
    print(n)
