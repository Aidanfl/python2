# This script demonstrates how to multiply a list by an integer to repeat its elements.
# It creates a list 'list1' containing a single element [0].
# The list is then multiplied by 4, resulting in the list being repeated 4 times, and the result is printed.

list1 = [0] 
print(list1 * 4)
