# This script prints the index value alongside the corresponding character for each character in the string "banana".
# It uses a loop to iterate through the string, printing both the index and the character at that index.

s = "banana"
#print the index value together with the corresponding character on the
index = 0
for ch in "banana":
    print(index, ch)
    index = index + 1
