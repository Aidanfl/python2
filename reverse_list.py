# This script reverses the order of elements in a given list.
# It uses the reverse() method to reverse the list in place, and then prints the reversed list.

aList = [100, 200, 300, 400, 500]
aList.reverse()
print(aList)
